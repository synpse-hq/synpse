name: {{ .SynpseTemplateID }}
scheduling:
  type: Conditional
  selectors: {}  
spec:
  containers:
    - name: cog
      image: {{ .CogImage }}
      gpus: all
      ports:
        - {{ .Port | default "5000" }}:5000                 
---
usage: |
  {{ .Usage }}



